services:
  ai-security:
    build: .
    container_name: ai-security
    env_file:
      - .env
    volumes:
      # :ro makes the mount read-only, preventing the deadlock
      - .:/app:ro
    command: python app/main.py